# Required: an OpenAI-compatible chat completions key.
# Default points to BigModel (智谱 GLM). Any OpenAI-compatible provider works.
OPENAI_API_KEY=

# Optional overrides (defaults shown)
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
OPENAI_MODEL=glm-4-plus
